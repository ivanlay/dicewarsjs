/**
 * Legacy configuration stub.
 *
 * Configuration is now managed in src/utils/config.js. This file remains
 * for backward compatibility and allows overriding settings before the
 * modern modules load.
 */
window.GAME_CONFIG = window.GAME_CONFIG || {};
